# Optional: protect endpoints with X-API-Key
API_KEY=

# Optional: use a real Claude model (worker)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-latest

# Advanced overrides (defaults are fine for local dev)
PUBLIC_HOST=localhost
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/agent
REDIS_URL=redis://redis:6379/0
COMPUTER_USE_IMAGE=ghcr.io/anthropics/anthropic-quickstarts:computer-use-demo-latest

# Postgres container
POSTGRES_PASSWORD=postgres
POSTGRES_DB=agent
